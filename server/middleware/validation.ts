import express, { NextFunction, Request, Response } from 'express';

export default function validateIncomingFields(
  req: Request, 
  res: Response, 
  next: NextFunction
) {
  // check field types exist on a specific method
  // check fields have the correct types
  
}